version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - revive
    - ineffassign
    - errcheck
    - unused
  settings:
    revive:
      severity: warning

formatters:
  enable:
    - gofmt
    - goimports