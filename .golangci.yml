version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - revive
    - ineffassign
    - errcheck
    - unused

formatters:
  enable:
    - gofmt
    - goimports

linters-settings:
  revive:
    severity: warning

issues:
  exclude-use-default: false